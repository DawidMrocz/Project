@model List<Aplikacja.DTOS.CatDto.CatDTO>
@{
    ViewData["Title"] = "Privacy Page";
}

<body>
    @foreach (var obj in Model)
    {
            <div class="line">
                <div class="jobId_content"> @obj.Year</div>
                <div class="jobId_content"> @obj.Month</div>
                <div class="jobId_content"> @obj.CatId</div>
                <a asp-action="Cat" asp-controller="Cat" asp-route-id="@obj.CatId">See cat</a>
            </div>
    }
</body>
